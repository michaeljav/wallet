# Base de datos Mongo para entorno local (sin Docker)
# Usa Mongo sin autenticación en localhost
MONGO_URI=mongodb://walletroot:walletpass@localhost:27017/walletdb?authSource=admin

# Correo temporal con Ethereal
# Email for local dev
# Option A (recommended): use local MailHog so flow matches Docker
MAIL_TRANSPORT=smtp
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
# SMTP_USER=   # leave empty for MailHog
# SMTP_PASS=
MAIL_FROM=no-reply@wallet.local
EXPOSE_TOKENS=true

# Puertos y rutas
WALLET_DB_PORT=3001
WALLET_API_PORT=3000
CLIENT_PORT=5173
WALLET_DB_BASE_URL=http://localhost:3001
VITE_API_BASE_URL=http://localhost:3000
